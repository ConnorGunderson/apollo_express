{
  "sourceRoot": "apps/apollo-express-client",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/apollo-express-client",
        "outputPath": "dist/apps/apollo-express-client"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "apollo-express-client:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "apollo-express-client:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "apollo-express-client:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "apollo-express-client:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/apollo-express-client"],
      "options": {
        "jestConfig": "apps/apollo-express-client/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/apollo-express-client/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
